import * as React from 'react'
import TimePicker from '../../src'

export default class Demo extends React.Component <any, any> {
    render() {
        return (
            <TimePicker input={{label:'输入框有的我也有'}}/>
        )
    }
}